# Contributing

Changes must preserve explicit release states, human approval, rollback readiness, deterministic offline examples, and tests. New integrations should not turn the reference implementation into an opaque provider-specific wrapper. Include tests for release blockers, missing evidence, rollback failure, incident ownership, and approval-bypass attempts.
