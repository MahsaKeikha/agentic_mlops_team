# F32 Tools

Executable MLOps tools live in [`src/tools/`](../src/tools/).

The tool layer supports build inspection, registry/release artifacts, operational checks, observability signals, and rollback preparation. Tools are deterministic capabilities invoked by agents through reusable skills.

See [`src/tools/domain_tools.py`](../src/tools/domain_tools.py).